// the node.js equivalent of PrintLine in Java
console.log(); // global object - can access at any setTimeout(function () {

setTimeout(); // start's timmer
clearTimeout(); // stop's timer

setInterval(); // repeatedly calls an object
clearInterval(); // stops calling object repeatedly

window // represents global scope
  window.console.log();
  window.setInterval();

var message = '';
  window.message;

// all of the above can be accessed in global...
  global.console.log();
